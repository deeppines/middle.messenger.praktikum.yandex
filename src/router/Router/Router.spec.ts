import { expect } from 'chai';
import jsdom from 'mocha-jsdom';

describe('Router', () => {
  jsdom({ url: 'http://localhost' });

  before(() => {
    return;
  });

  it('Router start should be return true', () => {
    return expect('');
  });
  it('Router start should be return true', () => {
    return;
  });
  it('Router start should be return true', () => {
    return;
  });
  it('Router start should be return true', () => {
    return;
  });
});
